# Diagrams

Three images referenced from the root `README.md`:

- `architecture.png` — system architecture (client → hosting → application → data → background/simulated services → CI/CD)
- `tech-stack.png` — technology stack grouped by layer
- `project-structure.png` — monorepo folder/file tree (backend + frontend workspaces)

All three were generated with an AI diagram tool from prompts describing this repo's actual
architecture, tech stack, and folder layout. If the codebase changes enough that a diagram goes
stale, regenerate it with an updated prompt (ask Claude Code for one — it will build the prompt
from the current README/codebase) rather than hand-editing the image.
